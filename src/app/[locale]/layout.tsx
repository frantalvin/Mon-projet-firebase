// Ce fichier et l'ensemble du dossier src/app/[locale] peuvent être supprimés.
// La structure de routage localisée a été retirée pour repartir de zéro.
// Un nouveau layout racine est créé à src/app/layout.tsx.
export default function ObsoleteLocaleLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
