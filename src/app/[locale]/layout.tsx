// This file and the entire src/app/[locale] directory can be deleted.
// The localized routing structure has been removed to start from a clean slate.
// A new root layout is created at src/app/layout.tsx.
export default function ObsoleteLocaleLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
